# Xinyu LED Studio V20.8.5 修補與驗證報告

日期：2026-09-02

## 已修補

1. 修復 `engineeringUnit` 未初始化造成的 P0 啟動中斷。
2. 修復 `libraryCurrentTab` 在初始化前存取的 TDZ 問題。
3. 修復圖層頁籤將 CSS selector 傳給 `getElementById` 所造成的 `classList` 錯誤。
4. 預設工作區、場景與背景統一為 1200 × 1200；舊專案缺少尺寸時會安全正規化。
5. 修正中小型桌面視窗下 Dashboard 與檢查器遮擋、頂部控制列換行覆蓋內容等互動問題。
6. AI 回應改以純文字渲染，避免遠端內容插入 HTML；遠端 API 加入 HTTPS／本機例外、30 秒逾時、2 MB 回應上限、無憑證與無 referrer。
7. 新增 CSP、Permissions Policy、no-referrer；Service Worker 不再快取跨來源的非核心資源。
8. 匯入專案的 ID 與狀態在 Dashboard 顯示前經過 HTML 安全轉義。
9. 版本與快取識別同步升級為 V20.8.5，避免舊快取混用。

## 瀏覽器驗證

- 全新來源啟動成功，無致命錯誤覆蓋層。
- 建立新專案成功。
- 素材庫 7 個分類載入；LED 模型搜尋可用。
- 「常規屏」可加入專案；圖層分類可顯示已建立模型。
- 工程單位可切換至 cm。
- 可新增第二場景；場景尺寸保持 1200 × 1200。
- 1280px 寬度下建立專案按鈕可點擊，Dashboard 不再被右側面板攔截。

## 條件式功能

影片／音訊格式支援取決於目標瀏覽器 codec；3D 引擎與遠端 AI 需要可連線至相應 CDN／API，正式部署仍應依網域設定 CORS、TLS 與 API 權限。
